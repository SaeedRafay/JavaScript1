"use strict";

const calculateDogAge = (humanYear) => {
    const dogYears = humanYear * 7;
    console.log(`Your doggie is ${dogYears} years old in dog years!`)
};

calculateDogAge(1);
calculateDogAge(2);
calculateDogAge(3);
